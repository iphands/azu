# CUDA/HIP deferred changes from big-fix CPU-only execution

Status: deferred; not compiled in this run

This dossier is the audit memory for every CUDA/HIP parity finding that the
CPU-only `big-fix` run must not implement. Nothing below was compiled, linked,
runtime-tested, or parity-verified in this run. Rows are created by the todo
that discovers the finding and finalized by the dossier-finalization todo.
This file is the Todo 3 durable scaffold; later todos expand and finalize it.
Path legend (repo-relative; inside a cell `cuda:`/`hip:` mean the CUDA/HIP twin of the focused file): `@V.cu`/`@V.hip`/`@V.cpp`/`@V.h` = `src/tsdf/TSDFVolume_cuda.cu`/`_hip.hip`/`src/tsdf/TSDFVolume.cpp`/`include/tsdf/TSDFVolume.h`; `@M.cu`/`@M.hip`/`@M.cpp`/`@M.h`/`@MT.h` = `src/meshing/MarchingCubes_cuda.cu`/`_hip.hip`/`src/meshing/MarchingCubes.cpp`/`include/meshing/MarchingCubes.h`/`include/meshing/MarchingCubesTables.h`; `@I.cu`/`@I.hip`/`@I.cpp`/`@I.h` = `src/tracking/ICPTracker_cuda.cu`/`_hip.hip`/`src/tracking/ICPTracker.cpp`/`include/tracking/ICPTracker.h`; `@S.cu`/`@S.hip`/`@S.omp`/`@S.h` = `src/sensor/SignalConditioner_cuda.cu`/`_hip.hip`/`src/sensor/SignalConditioner_omp.cpp`/`include/sensor/SignalConditioner.h`; `@R.cu`/`@R.hip`/`@R.cpp` = `src/sensor/SuperResolution_cuda.cu`/`_hip.hip`/`src/sensor/SuperResolution.cpp`; `@PC.cpp` = `src/app/PipelineController.cpp`; `@FD.cpp` = `src/sensor/FrameData.cpp`; `@P.cpp` = `src/sensor/Preprocessor.cpp`; `@K.h` = `include/sensor/KinectSensor.h`; `@FP.h` = `include/app/FusionHyperparams.h`; `@CUP.h` = `include/utils/CudaUniquePtr.h`; `@IS.h` = `include/tracking/ICPShared.h`.

CPU line-number drift from big-fix Todo 15: `@V.cpp` gained lines in
`sameParams()`, `integrateCPU()` and a rewritten `raycast()`, and `@V.h` grew
`TSDFParams`, so the CPU `@V.cpp:`/`@V.h:` numbers cited in rows below lag by a
symbol-dependent offset (`@V.cpp`: +2 for old lines 41-255, +1 for 256-271, +4
for 272-395, +8 for 396-398, +22 for 399-426, +16 from 427 on; `@V.h`: +7 from
line 38). Treat the symbol names each row names as the durable anchor, and
re-derive the line before editing a backend. The CPU's hard-coded raycast lines
(`t = 0.3f`, `while (t < 5.0f)`, the `vs`-step interpolation) are gone entirely.

## Environment at deferral time

Measured on this host by `scripts/big-fix-env-probe.sh` plus direct PATH probes
(see `.omo/evidence/big-fix/backend-deferral-scaffold.txt` for the raw output;
`required_cpu_lane: pass`):

- nvcc: missing (`command -v nvcc` exit 1, `nvcc --version` exit 127)
- hipcc: present on PATH at `/usr/sbin/hipcc` but not usable as a versioned
  compiler: `hipcc --version` reports `/usr/share/hip/version` not found and
  `/usr/bin/rocm_agent_enumerator` is absent, so the version string is unknown
- CUDA runtime: present at driver level (`libcuda.so.1 => /lib64/libcuda.so.1`,
  `/dev/nvidia0`, `/dev/nvidiactl` exist) — recorded, and still unreachable
  because `nvcc` is absent and this plan forbids CUDA compilation
- HIP runtime: absent at runtime level (no `libamdhip64` in the `ldconfig`
  cache, no `/dev/kfd`) even though the `hipcc` wrapper is on PATH
- libfreenect: present (`pkg-config --exists libfreenect` exit 0; the probe
  field `libfreenect: 0` is a modversion-string artifact, the real soname is
  `libfreenect.so.0.7.5`)
- existing build-cuda/: present — protected; never deleted, cleaned,
  reconfigured, or otherwise mutated by this plan

## Future backend gate

- DO NOT RUN IN THIS PLAN: cmake -S . -B build-cuda-big-fix -G Ninja -DGPU_BACKEND=CUDA -DBUILD_TESTING=OFF
- DO NOT RUN IN THIS PLAN: cmake --build build-cuda-big-fix
- DO NOT RUN IN THIS PLAN: cmake -S . -B build-hip-big-fix -G Ninja -DGPU_BACKEND=HIP -DBUILD_TESTING=OFF
- DO NOT RUN IN THIS PLAN: cmake --build build-hip-big-fix

A future backend run starts from this dossier, explicitly permits `nvcc` /
`hipcc` compilation, and must not treat any CPU-only test as proof that a
backend translation unit compiles. `GPU_BACKEND=AUTO` is also forbidden in CPU
QA on this host: with `hipcc` on PATH, AUTO can select HIP.

## Deferred change table

| Audit ID | Backend | File/function | Current CPU decision | Required CUDA/HIP change | Risk if not done | Future acceptance |
|---|---|---|---|---|---|---|
| `meshing:D1` | CUDA | `@M.cu` file-scope `static const int edge_table[256]` (line 35), `static const int tri_table[256][16]` (line 70), `__constant__ int c_edge_table[256]` (line 332), `__constant__ int c_tri_table[256][16]` (line 333), `cudaMemcpyToSymbol` copies (lines 517-518) | CPU consolidation is DONE: the CPU path consumes the single shared header `@MT.h` (`kfusion::meshing::tables::{edge_table, tri_table}`, `inline constexpr`). The CUDA copy is a separate hand-copied duplicate that still does NOT consume the shared header; it was left untouched in this run (deferred, not compiled, not runtime-tested) | Delete the local table definitions and `#include` the shared CPU table header, keeping only the `__constant__` copies fed by `cudaMemcpyToSymbol` from the shared arrays | Two table copies drift; the CUDA meshing output silently diverges from the CPU canonical mesh (today the copies are value-identical, incl. the corrupt 213/214/215 entries; they diverge the moment Todo 7 fixes only the shared header) | Future CUDA run: compile the gate above, then diff CUDA meshing output against the CPU-canonical Marching Cubes fixture byte-for-byte within the documented backend tolerance |
| `cross-backend:A8` | HIP | `@M.hip` file-scope `static const int edge_table[256]` (line 31), `static const int tri_table[256][16]` (line 66), `__constant__ int c_edge_table[256]` (line 328), `__constant__ int c_tri_table[256][16]` (line 329), `hipMemcpyToSymbol` copies (lines 515-516) | Same shared-header decision as `meshing:D1`; CPU shared header is live, the HIP copy remains separate and untouched (deferred, not compiled, not runtime-tested). Verified by grep: the HIP file carries no special "fix" comment; it is a plain hand-copied table | Same shared-header include for the HIP TU; delete the hand-copied arrays so a single CPU table edit reaches every backend | Hand-copied-table drift: a CPU table correction (e.g. Todo 7's 213/214/215 fix) never reaches the HIP kernel until this migration lands, and the divergence is invisible without a HIP build | Future HIP run: compile the gate above, then diff HIP meshing output against the CPU-canonical Marching Cubes fixture |
| `cross-backend:A7` | CUDA + HIP | `@M.cu` / `@M.hip` `edge_table` entries 213/214/215 = `0x835`/`0xb3f`/`0xa36` (corrupt in both copies; canonical reference is `0x83f`/`0xb35`/`0xa3c`) | NOT yet fixed anywhere: the shared CPU header deliberately carries the same corrupt values byte-for-byte (Todo 4 was a value-preserving move); big-fix Todo 6 adds the failing analytic tests and Todo 7 corrects the shared header only. Backend copies untouched, still corrupt (deferred, not compiled, not runtime-tested) | After Todo 7, port the corrected `edge_table` entries 213/214/215 by consuming the shared header instead of patching the copies | Non-manifold/holes in backend meshes (and in today's CPU meshes); divergence is invisible without a backend build | shared header compiles into each backend, and backend mesh equals the CPU-canonical mesh for the cubes covering indices 213/214/215 |
| `pipeline:PC-02` | HIP | `@PC.cpp` integration loop, `#elif defined(HIP_ENABLED)` `else { // Fall through to CPU raycast }`; `emitCpuPreview` is compiled out under `#ifndef HIP_ENABLED` | deferred, not compiled, not runtime-tested. CPU always raycasts through `emitCpuPreview()`, so a published model frame is always a real model frame | In the HIP `use_gpu_ == false` branch, actually run the CPU raycast and publish a preview (share `emitCpuPreview()` with CUDA/CPU) instead of an empty else that still hits `model_buffers_.swap()` / `model_ready_.store(true)` | HIP GPU-disabled runs publish a stale/unwritten model buffer and tracking consumes a phantom model frame | Future HIP run: compile the gate above, then run a HIP GPU-disabled smoke scenario and assert `model_ready_` implies a raycast-written buffer, compared against the CPU-canonical publish path |
| `cross-backend:A5` | CUDA + HIP | `@S.cu` / `@S.hip` `rawToMetersKernel`, `fillDepthHolesKernel`, `denoiseDepthSpatialKernel` | deferred, not compiled, not runtime-tested. CPU depth-validity band is canonical (the CPU depth-validity todo owns the raw-band contract); GPU preprocessing untouched | GPU kernels must reproduce the CPU canonical invalid/hole band and the corrected raw-to-meters curve instead of the current raw-2047-as-hole rule | GPU manufactures geometry from invalid depth that the CPU path rejects | compile the gate above, then compare GPU preprocessed depth against the CPU-canonical depth-validity table over the full 0..2047 raw band |
| `sensor:S-13`, `cross-backend:A21`, `cross-backend:A22` | CUDA + HIP | `@S.cu` / `@S.hip` `SignalConditioner::processCuda`, `bilateralDenoiseRgbKernel` | deferred, not compiled, not runtime-tested. CPU signal-conditioner is canonical; GPU conditioning untouched | Add the missing divide/clamp guards in the GPU bilateral path and make `processCuda` report failure instead of returning success unconditionally | Silent GPU preprocessing failure feeds unconditioned frames into tracking and integration | compile the gate above, then assert GPU conditioning equals the CPU-canonical conditioner output and that injected allocation failures propagate as `false` |
| `cross-backend:A23` | CUDA | `src/meshing`/`src/sensor` CUDA path: `@S.cu` `applyCASKernel` launch and `CUDA_CHECK` coverage of the RCAS calls | deferred, not compiled, not runtime-tested. CPU CAS path is canonical; unchecked backend calls are recorded, not patched | Check every RCAS/CAS CUDA API return and propagate failure to the caller | Unchecked CUDA API errors leave a half-written frame that still reaches the consumer | Future CUDA run: compile the gate above, then fault-inject each CAS call and assert the CPU-canonical error contract |
| `sensor:S-05`, `cross-backend:A20` | CUDA + HIP | `@R.cu` `reflectCoordCAS`/`rcasGetPixel`; `@R.hip` `reflectCoordEASU`/`rcasGetPixel` | deferred, not compiled, not runtime-tested. CPU CAS border handling (the CPU CAS todo) is canonical; backend border mode untouched | Align GPU border addressing with the CPU canonical border mode (reflect vs clamp) in both backends | Border pixels differ frame-to-frame between CPU and GPU previews; edge artifacts in the UI and in ICP | compile the gate above, then compare a fixed CAS input border ring against the CPU-canonical CAS output |
| `sensor:S-06`, `cross-backend:A24` | HIP | `@R.hip` `applyEASU_GPU`, `easuKernel` | deferred, not compiled, not runtime-tested. CPU super-resolution contract is canonical (the CPU SR-contract todo); HIP EASU untouched | Fail loudly when the EASU allocation fails instead of continuing with an unwritten buffer | HIP allocation failure yields a black frame that is consumed as if it were valid | Future HIP run: compile the gate above, then fault-inject the EASU allocation and compare behavior with the CPU-canonical SR failure contract |
| `cross-backend:B5`, `cross-backend:C8` | CUDA + HIP | `@R.cu` `rcasKernel`; `@R.hip` `rcasKernel` | deferred, not compiled, not runtime-tested. CPU sharpness clamp and NaN handling are canonical; GPU kernels untouched | Make the GPU sharpness/NaN clamp symmetric with the CPU canonical clamp and remove the dead upscaled-RGB feature path | Asymmetric sharpening and NaN propagation on GPU; dead feature misleads future backend work | compile the gate above, then compare RCAS output on a NaN/edge fixture against the CPU-canonical RCAS reference |
| `cross-backend:B4` | HIP | `@R.hip` `static HipBufferRAII g_easu_buffers`, `static HipBufferRAII g_rcas_buffers` | deferred, not compiled, not runtime-tested. CPU has no process-lifetime GPU singleton; nothing to change in this run | Replace the process-lifetime singletons with explicitly scoped, teardown-ordered buffers | Static-destruction-order teardown against a torn-down HIP context; leaks across re-init | Future HIP run: compile the gate above, then run repeated init/shutdown cycles and assert buffer lifetime is bounded and teardown ordering is deterministic |
| `tsdf:T1`, `tsdf:T12`, `tsdf:T17`, `cross-backend:A33` | CUDA + HIP | `@V.cu` / `@V.hip` `initGPU`, `freeGPU`, `syncToGPU`, `syncFromGPU`, `extractGlobalPointCloudGPU`, `classifyVoxelKernel`, `compactPointsKernel` | deferred, not compiled, not runtime-tested. CPU volume lifecycle guards are the canonical contract; GPU lifecycle untouched | Serialize the GPU global point-cloud buffers, fix the index/footprint overflow, and make init/free/sync idempotent and re-entrant | Data race or integer overflow corrupts the GPU point cloud; double-init leaks device memory | compile the gate above, then run the CPU-canonical lifecycle scenario (init/free/repeat, concurrent extract) on the backend and compare the extracted point set with the CPU canonical volume |
| `cross-backend:B3` | CUDA + HIP | `include/tsdf/VoxelGPU.h` `VoxelGPU::padding[3]` | deferred, not compiled, not runtime-tested. CPU voxel layout is canonical; the GPU struct is untouched | Document or remove the 12-byte padding and assert `sizeof(VoxelGPU)` in the backend build | Unaccounted 12 bytes per voxel multiplies to megabytes of VRAM; layout drift breaks any host/device memcpy | compile the gate above with a `static_assert` on `sizeof(VoxelGPU)` and compare the footprint against the CPU-canonical voxel size |
| `tsdf:T10`, `tsdf:T15`, `meshing:C8`, `cross-backend:A13`, `cross-backend:A26` | CUDA + HIP | `include/tsdf/VoxelGPU.h` `r`/`g`/`b` float accumulators; `@M.cu` `(uint8_t)fminf(255.0f, fmaxf(0.0f, ...))` vs `@M.hip` "simple cast without clamping" | deferred, not compiled, not runtime-tested. CPU color contract is canonical (float sRGB `[0,1]` in the volume, uint8 sRGB out of meshing, linear float into GLB); GPU storage untouched | Give both backends one shared, clamped, non-UB float-to-uint8 conversion and the same accumulator rounding as CPU | CUDA and HIP already clamp differently from each other; undefined float-to-uint behavior and per-backend color drift | compile the gate above, then compare exported mesh colors against the CPU-canonical color contract fixture for both backends |
| `tracking:GPU-3`, `tracking:GPU-6`, `cross-backend:A6`, `cross-backend:A9`, `cross-backend:A16` | CUDA + HIP | `@I.cu` / `@I.hip` ICP `damping` (`0.1f`, level-0 `0.01f`, escalation `1.0f`), normal gate, residual weighting | deferred, not compiled, not runtime-tested. CPU ICP policy (adaptive Tikhonov, non-finite rejection, translation/rotation caps, determinant-corrected SO(3) projection, shared validity gates, weighted Huber objective) is canonical and implemented in `@IS.h` + `@I.cpp` by big-fix Todo 13; backend ICP untouched | Port the CPU canonical damping escalation, validity gates, SO(3) projection, residual weighting, and step guards to both backends | GPU tracking converges to different poses than CPU on ill-conditioned scenes; parity is unprovable | compile the gate above, then compare backend ICP pose/energy traces against the CPU-canonical ICP fixtures within the documented tolerance |
| `tsdf:T7`, `tsdf:T16`, `cross-backend:A11`, `cross-backend:A12`, `cross-backend:A27` | CUDA + HIP | `@V.cu` / `@V.hip` `raycastKernel`, dead `min_depth` | deferred, not compiled, not runtime-tested. The CPU raycast these IDs must converge on is now implemented and locked (Todo 15): band-scaled march bounds, uniform half-voxel step, interpolation over the step actually taken, entry and exit crossings, non-finite abort, all three outputs written together, color only from the fused voxel the hit falls in. The GPU kernel is untouched: hard-coded `0.3f`/`5.0f`, the legacy `vs`-step interpolation and the dead `min_depth` all remain | Match the CPU canonical step interpolation, add the NaN guards, and remove or honor the dead `min_depth` | GPU model frames differ from CPU in depth and validity, which then feeds tracking divergence | compile the gate above, then compare backend raycast output against the CPU-canonical fixtures `tests/tsdf_raycast_contract.cpp` and `tests/tsdf_subvoxel_thin_feature_contract.cpp` |
| `meshing:B5`, `cross-backend:A1` | CUDA | `@M.cu` triangle emission order in the meshing kernel (`for (int k = 0; k < 3; ++k)`, forward) | deferred, not compiled, not runtime-tested. CPU winding convention is now DERIVED and LOCKED (big-fix Todo 18): under the signed-distance sphere fixture in right-handed world space, only the reverse table order `(2, 1, 0)` yields positive signed volume and strictly outward face normals (`tests/marching_cubes_winding_contract.cpp`, derivation in `.omo/evidence/big-fix/winding-derived-sign.txt`); the CUDA forward order `(0, 1, 2)` is mathematically inward under the same fixture. CUDA winding untouched | Make CUDA triangle emission use the derived outward order: reverse table order `(2, 1, 0)` under the shared table incidence and the `tsdf < 0` inside convention | CUDA meshes are inside-out relative to CPU/HIP | Future CUDA run: compile the gate above, then replay the Todo 18 fixture rule (positive signed volume, strict per-face radial outward test, per-cube both-candidate derivation) on the CUDA mesh; forward order must fail it |
| `tracking:CPU-1` parity, `cross-backend:A18`, `cross-backend:A19` | CUDA + HIP | backend model-pixel rounding and `worldToVoxel` truncation paths | deferred, not compiled, not runtime-tested. CPU rounding/floor semantics are canonical; backend conversion untouched | Use the CPU canonical rounding/floor in the backend conversion helpers | Sub-voxel pose and projection drift that grows with distance | compile the gate above, then compare backend voxel-index traces against the CPU-canonical conversion table |
| `meshing:D3`, `cross-backend:A29` | HIP | `@M.hip` distance-validated weld branch (`merge_threshold`, "create new vertex despite hash collision") | deferred, not compiled, not runtime-tested. CPU welding is now EXACT edge-key identity (big-fix Todo 17: `MarchingCubes::crossingFromLower` + an `EdgeKey` weld in the serial merge, locked by `tests/mesh_welding_contract.cpp`); the HIP quantized weld and its defensive branch are untouched | Remove the dead defensive branch or make it reachable under the CPU canonical hash-collision policy | Dead branch reads as protection that never fires; future HIP weld work is misinformed | Future HIP run: compile the gate above, then prove the branch is reachable (or delete it) and compare welded vertex counts with the CPU-canonical weld fixture |
| `cross-backend:C1` | HIP | `src/*/*_hip.hip` (all five HIP translation units, never compiled by the current CUDA build) | deferred, not compiled, not runtime-tested. This run compiles none of them; they are recorded as never-compiled in this plan | Establish a real HIP compile gate before trusting any HIP parity claim | All HIP findings above are simultaneously unverified and unbuildable | Future HIP run: the HIP compile gate above passes and every `*_hip.hip` TU appears in `compile_commands.json` |
| `cross-backend:C4`, `cross-backend:C5`, `cross-backend:C6`, `cross-backend:C7` | CUDA + HIP | `@R.cu` EASU half absent vs the HIP EASU/RCAS pair; hygiene nits (full list in `.omo/evidence/big-fix/backend-verification-hygiene-cmake.txt`): `@M.cu:4`+`:10` duplicate `#include "tsdf/VoxelGPU.h"`, `@M.cu:513`/`.hip:511` stale `// Tables from MarchingCubes.cpp`, `@V.cu:33` "Image-Centric (Pixel Parallel)" above `integrationKernel_VoxelParallel` (`:35`, per-voxel at `:190`), `@V.cu:137` "0-255 range" vs `REGRESSION_REVIEW.md:13`, `@M.hip:13`/`:21` `HIP_CHECK` printing `"CUDA error"`, `@S.cu:475`/`:482` two steps numbered `5` | deferred, not compiled, not runtime-tested. CLAIM CORRECTED: the draft claim that backend-guarded declarations have "no backend definition" is FALSE and withdrawn - every backend-guarded block in `include/{tsdf,meshing,sensor,tracking}/*.h` has a definition (`@V.cu:107,118,126,146,171,358,445` + HIP twins, `@M.cu:501,525`, `@I.cu:280,289,297,340`, `@S.cu:413`, `@R.cu:98`), and `include/app/*.h` has none. The genuinely dangling declarations are the NON-guarded ones: `MarchingCubes::extractGPU` (`@M.h:29`, defined only at `@M.cu:533`/`.hip:531`) and `ICPTracker::projectModel` (`@I.h:167`, zero definitions, zero call sites). `applyEASU_GPU` is intentionally HIP-only (`include/sensor/SuperResolution.h:27-31`). CPU dead-code cleanup stays on the CPU path; backend dead code is named here, not edited| Finish or delete the dead upscaled-RGB feature, correct the stale comments, dedupe the include, move the meshing tables onto `@MT.h`, delete or define `extractGPU`/`projectModel` | A future run ports against stale comments and one doc claim that is simply false | Compile the gate, show each named symbol defined-and-used or deleted and each named comment corrected or gone |
| `cross-backend:C3` | HIP | `CMakeLists.txt` `GPU_BACKEND=AUTO` detection (prefers HIP, hard-coded `gfx90c`) | deferred, not compiled, not runtime-tested. Verified ordering: the HIP probe `CMakeLists.txt:59-77` runs first and CUDA is gated behind `NOT HIP_AVAILABLE` at `:80`, so AUTO prefers HIP; `set(CMAKE_HIP_ARCHITECTURES gfx90c)` at `:70` is an unconditional `set()` that shadows any `-DCMAKE_HIP_ARCHITECTURES=...` cache entry, unlike CUDA which is guarded by `if(NOT DEFINED ...)` at `:51-54`. A repo-wide grep for `gfx\|offload-arch\|rocm-smi\|hipGetDeviceProperties\|amdgcn` matches only `:70` - no device query anywhere | Report the real target architecture or fail explicitly, and stop clobbering an explicit `CMAKE_HIP_ARCHITECTURES` | AUTO silently targets the wrong AMD silicon, and a user passing the correct `--offload-arch` still gets `gfx90c` | Compile the gate with explicit `--offload-arch` and assert the reported architecture matches the device |
| `cross-backend:C2` | CUDA + HIP | `CMakeLists.txt:56-97` probe, `:173-181` globs, `:210-215` macros; `build-cuda/compile_commands.json` | deferred, not compiled, not runtime-tested. Mechanism corrected (the old wording was imprecise): the CUDA glob is gated on `CUDA_AVAILABLE` and the HIP glob on `HIP_AVAILABLE`, and `:80` makes them mutually exclusive, so at most one glob ever fires; every backend TU is also wrapped end-to-end in its own macro. On-disk proof: `build-cuda/compile_commands.json` has 33 entries, 5 `.cu`, 0 `.hip`; no `build-hip*/` exists | Give each backend a real documented compile gate and record which glob fired, so 'not globbed' is never mistaken for 'compiles' | A future run assumes the HIP files build; they have never been compiled in any configure on this host | Each backend's `compile_commands.json` lists its five TUs; each backend file appears in exactly one configure |
| `tsdf:T1` null-pointer variant | CUDA + HIP | `@V.cu:459`/`hip:456` `extractGlobalPointCloudGPU` launch; `@M.cu:544`, `:566`/`hip:542`, `:567`; `@V.h:117`, `:140`, `:144`; `@V.cpp:105`, `:108` | deferred, not compiled, not runtime-tested. `integrateGPU` (`cuda:180`), `raycastGPU` (`:364`) and the syncs (`:127`, `:147`) guard on `gpu_valid_`; `extractGlobalPointCloudGPU` does not, and `MarchingCubes::extractGPU` consumes the same pointer as an unchecked `void*`. `setParams` calls `freeGPU()` on a resolution change, nulling `d_voxels_` and clearing `gpu_valid_` but never `gpu_enabled_`, and never re-arms `initGPU()`; `isGPUEnabled()` has zero callers, so the desync is invisible. GUI-reachable: `src/gui/ControlPanel.cpp:171-174`, `:269` -> `@PC.cpp:220` | Add the `gpu_valid_` guard to the extraction, re-init or invalidate in `setParams`, stop handing out an unchecked `void*` | Kernel launch on a null device pointer after a resolution change, where CPU merely degrades | Compile the gate, drive a resolution change mid-session, assert no launch while `gpu_valid_` is false |
| `tsdf:T2`, `tsdf:T5`, `tsdf:T11`, `tsdf:T24` | CUDA + HIP | `@V.cu:165`, `:216`, `:218`, `:227`, `:301`, `:347` / `hip:161`, `:212`, `:214`, `:223`, `:297`, `:344`; `@V.h:26-30`, `:40-44` | deferred, not compiled, not runtime-tested. CPU canonical empty sentinel ESTABLISHED: `@V.h:26-30` names `EMPTY_TSDF = 1.0f`, `EMPTY_WEIGHT = 0.0f`, `EMPTY_COLOR = 128`, the `@V.h:40-44` struct defaults consume them under a `static_assert`, and the reset fill `@V.cpp:87-91` plus the `getTSDF` / raycast unobserved samples use them; `tests/tsdf_reset_contract.cpp` locks the CPU side. The backends are untouched and still split one 'empty voxel' state over three literals, the device->host sync marker being `1.0f` for `weight <= 0.001f` (which still writes `weight` and keeps stale host RGB). No `EMPTY_TSDF`/sentinel constant exists in any `.cu` or `.hip`, and emptiness is still tested by two different weight thresholds (`> 0.0f` in `getTSDF`, `<= 0.001f` in meshing) | One named empty-voxel constant and one named weight epsilon, used on CPU and both backends | The CPU empty-voxel fix never reaches the backends, so the device sync marker and the meshing epsilon keep disagreeing with the CPU canonical constant | Compile the gate and assert the backend empty-voxel fill equals the CPU canonical constant |
| `tsdf:T8` | CUDA + HIP | `@V.cu:41` (signature), `:72`, `:63` / `hip:37`, `:68`, `:59`; launch `cuda:200` / `hip:196` | deferred, not compiled, not runtime-tested. The backend hard floor stands unchanged: `min_depth` is a kernel parameter that is passed and then referenced zero times in either kernel body, and both gate on the literals `d_meas < 0.1f` and `cz_c <= 0.1f`. CPU no longer carries that floor - Todo 15 deleted it, so `integrateCPU` gates the pixel and the march's near clamp on the `min_depth` and `max_depth` arguments its caller passes through `integrate()`, while `raycast` bounds its march from the mirrored `TSDFParams` band and reacts to `setHyperparams()` immediately. This is NOT a claim of a live integration slider: `integrationLoop()` snapshots the band once before its worker loop (`@PC.cpp:818-823`, current numbering) and passes those locals to every `integrate()` call, so a mid-session change reaches integration only after a restart - that lifecycle is Todo 24, not this row | Honor the configured `min_depth` in both backends, mirroring the CPU Z-depth clamp `max(min_depth * ray_scale, t_meas - truncation)`, or delete the parameter and document the hard floor | The user-visible control reaches the CPU raycast at once, CPU integration only after a restart, and neither backend at all, so a backend-vs-CPU sweep over the near band is not meaningful until this row and the Todo 24 lifecycle are both fixed | Compile the gate, sweep the band through the public `integrate()` arguments, and reproduce the CPU-canonical near-band fixture `tests/tsdf_integration_min_depth_contract.cpp` |
| `tsdf:T9` | CUDA + HIP | `@V.cu:312-315`, `:318-321`, `:330`, `:353-355` / `hip:309-312`, `:315-318`, `:327`, `:350-352`; buffer `@PC.cpp:119-121` | deferred, not compiled, not runtime-tested. Wording corrected: non-hits ARE zeroed, so the 'color written unconditionally' framing is wrong. The real stale-color mechanism is partial writes - `out_v`/`out_n` are written before the voxel-bounds guard at `:330`, so when it fails `out_c` keeps last frame's value; and both early returns write only `out_v`, leaving normals and colors from an earlier frame in the ping-ponged model buffer | Write all three outputs on every exit path, or clear the buffers before launch | Ghost color and stale normals wherever a hit is out of volume bounds or non-finite. CPU has no buffer reuse and, since Todo 15, writes the three outputs only together for a resolved finite hit and reads color only from the fused voxel the hit falls in (`tests/tsdf_raycast_contract.cpp`) | Compile the gate, run an out-of-bounds-hit fixture, compare color/normal with the CPU canonical raycast |
| `tsdf:T13` | CUDA + HIP | `@V.cu:146-169` / `hip:142-165`; `@V.h:99`, `:122`; `syncToGPU` call sites `cuda:123` / `hip:119` | deferred, not compiled, not runtime-tested. On CPU the host voxel mirror IS authoritative and is now never stale: `reset()` restores the canonical empty state and `setParams()` (`@V.cpp:93-115`) clears it on any parameter difference, locked by `tests/tsdf_reset_contract.cpp`. Backend side, verified by exhaustive grep over `src/`, `include/`, `tests/`, `scripts/`, `CMakeLists.txt`: only the two definitions and two declarations exist, zero call sites. Device->host voxel write-back therefore never runs, the `float` color accumulators (`cuda:98-100`) are discarded every frame, and it sizes itself from `voxels_.size()` (`cuda:148`) rather than the count recorded at `initGPU` | Wire it into a real readback path or delete it and document that host voxels are never authoritative after GPU integration | 24-25 lines of dead code per backend that read as a working sync API | Compile the gate and show the symbol either called-and-correct or deleted |
| `tsdf:T14`, `cross-backend:A33` (verified detail) | CUDA + HIP | `@V.cu:448-449`, `:49`, `:215`, `:226`, `:331`, `:400`, `:427`, `:451-454`/`.hip:445-446`, `:45`, `:211`, `:222`, `:328`, `:397`, `:424`, `:448-451`, `:461-474`; correct widening at `cuda:120` | deferred, not compiled, not runtime-tested. Extraction sizing and every kernel index computation is signed `int` (overflows at `res >= 1291`) while only the allocation widens to `size_t`; no entry point validates `res`. Footprint: four extra per-voxel device buffers ~= 28 B/voxel over the 32 B/voxel volume (~+3.76 GB at `res = 512`); HIP also allocates two ~536 MB host vectors with a D2H+H2D round trip per call where CUDA stays on device via `thrust::exclusive_scan` | Widen the count/index arithmetic, validate `res` at every entry point, drop the HIP-only host round trip | Silent integer overflow and multi-gigabyte allocation; CUDA and HIP have materially different memory profiles for one output | Compile the gate, run the maximum supported resolution on both backends, compare the point set with the CPU canonical volume |
| `tsdf:T19`, `tsdf:T26` | CUDA + HIP | `@V.cu:300`, `:304`, `:309`, `:345-349` / `hip:296`, `:300`, `:306`, `:342-343`; `@V.h:32-45` (`TSDFParams`); the CPU's own hard-coded `0.3f`/`5.0f` raycast lines no longer exist (Todo 15 rewrote `raycast()`) | deferred, not compiled, not runtime-tested. Divergence updated: the two backends still hard-code near/far as literals absent from `TSDFParams` and from the `raycastGPU` signature, and still detect only the positive->negative entry crossing - a ray starting inside negative material never resolves an exit there, and one empty sample (`1.0f`) resets the pending crossing. CPU now carries the band in `TSDFParams`, applies it as Z-depth scaled by the ray norm `norm(ray_cam)`, and detects both crossings, so this is no longer a shared blind spot. The backend volume extent is `256 x 0.010 m = 2.56 m`, so the backend `5.0f` far bound never binds | Carry near/far in `TSDFParams` and use them everywhere, and detect the exit crossing as the signed-volume rule requires | The backends keep the thin-shell and interior-start defects and now disagree with the CPU canonical raycast, so a backend-vs-CPU comparison sees real divergence instead of a shared bug | Compile the gate, compare backend raycast against the CPU canonical fixture including interior-start rays |
| `tsdf:T20` | CUDA + HIP | `@V.h:186-189` (CUDA) / `:203-206` (HIP); allocs `@V.cpp:142`, `:146` / `:165`, `:169`; `freeGPU` `@V.cu:107-116` / `hip:103-112` | deferred, not compiled, not runtime-tested. `freeGPU()` frees `d_voxels_` and the four `d_pc_*` buffers but not `d_depth_integ_`/`d_rgb_integ_`, and `last_depth_size_`/`last_rgb_size_` are never zeroed, so the reuse guard keeps them alive across a full free/re-init cycle; they die only with the object (`@CUP.h:15-21`) | Free the staging buffers and reset the cached sizes in `freeGPU()` | 'GPU freed' is false: device memory survives every `freeGPU()`, including `PipelineController::stop()` (`:281-282`, `:293-294`) | Compile the gate, assert device allocations return to baseline after `freeGPU()` |
| `tsdf:T21` | CUDA + HIP | `@V.cpp:150`, `:152` (discarded), `:173`, `:175` (`(void)`-cast); wrapped for contrast: `@V.cu:142`, `:153`, `:485`, `:486` and `hip:138`, `:149`, `:462`, `:474`, `:489`, `:490`; free swallow `@CUP.h:16-20` / `HipUniquePtr.h:16-20` | deferred, not compiled, not runtime-tested. Verified counts: the CUDA file wraps 4/4 copies, HIP 6/6, and `@V.cpp` 0/4 | Check and propagate those four copies; stop swallowing every free result | A failed upload feeds the previous frame's depth and RGB into integration as if it were fresh | Compile the gate, fault-inject each copy, assert the CPU-canonical error contract |
| `tsdf:T22` | CUDA + HIP | `@V.cu:118-124` `initGPU`; `@V.cpp:93-115` `setParams`; `:69-71` ctor | deferred, not compiled, not runtime-tested. three entry points, zero validation. `initGPU` sets `gpu_valid_ = true` unconditionally with no `res > 0`, no upper bound and no allocation check beyond the throw inside `make_cuda_unique`; `setParams` validates nothing about resolution, voxel size, truncation or max weight; the ctor computes `resolution^3` with no diagnostic | Validate the parameter set once at the shared entry point and fail loudly | A zero, negative or oversized resolution yields an empty or multi-gigabyte volume while `gpu_valid_` still claims the GPU is usable | Compile the gate, assert each invalid parameter set is rejected identically on all three paths |
| `tsdf:T27` | CUDA + HIP | `@V.cu` vs `@V.hip` depth and epsilon literals | deferred, not compiled, not runtime-tested. CLAIM WITHDRAWN AS FALSE: `diff -u` shows the depth constants are identical - `cz_c <= 0.1f` (`cuda:63`/`hip:59`), `d_meas < 0.1f` (`:72`/`:68`), `float t = 0.3f` (`:300`/`:296`), `while (t < 5.0f)` (`:304`/`:300`), `1e-6f`, the `0.001f` weight gate and the color clamp all mirror. The only non-API differences are comments and the prefix-scan strategy (`cuda:464-468` thrust vs `hip:460-474` host scan) | Nothing for this ID; the real CUDA/HIP asymmetries here are the scan (`tsdf:T14`) and the comment rot (`cross-backend:C4`) | A false 'constants disagree' row sends a future run to diff a diff that does not exist | Re-diff the two files and confirm only the API prefix and the scan strategy differ |
| `tsdf:GPU-OUT` | CUDA + HIP | `@V.cu:470` / `hip:472` vs CPU `@V.cpp:487-488`; caller `@PC.cpp:266` | deferred, not compiled, not runtime-tested. on a zero-point extraction the GPU returns with the caller's output vectors untouched while CPU clears them, and the pipeline reuses `global_frame->vertices`/`rgb` across calls | Clear the outputs before the early return so both paths share a postcondition | An extraction that shrank to empty silently keeps the previous run's vertices | Compile the gate, assert identical output state for a zero-point extraction |
| `meshing:C2` | CUDA + HIP | `@M.cu:454-456`, `:341-350`, `:465`, `:486-487`, `:612` / `hip:450-452`, `:337-346`, `:461`, `:484-485`, `:616`; CPU `@M.cpp:78`, `:100`, `:222`, `:226`, `:237`, `:254` | deferred, not compiled, not runtime-tested. Verified PARTIAL: a NaN guard does exist in both backends, but it substitutes `make_float3(0,0,0)` - the world origin - which is then emitted as a legitimate vertex; `isinf` appears nowhere in the three meshing files; `interpolateEdgeGPU` clamps `t` but not its inputs; emission and the host weld are unguarded; normals are never checked (divide-by-zero masked by `+ 1e-9f` inside the `sqrtf`). **CPU is FIXED (big-fix Todo 16)**: `@M.cpp:222` rejects a non-finite interpolated position, `:226` rejects a non-finite endpoint normal, `:237` rejects a blended normal that is non-finite or collapses below `kGradientEpsilon`, and `:254` drops every triangle row that touches a refused edge, so nothing is ever substituted; `sampleCorner` `@M.cpp:78` refuses a non-finite `tsdf` at the source. Locked by `tests/marching_cubes_frontier_contract.cpp` sections 4-6 | Reject non-finite vertices at emission on BOTH backends the way CPU now does, and test with `isfinite` rather than `isnan` | Every NaN edge collapse deposits a spurious vertex at the world origin that then welds into the real mesh, and only CPU refuses to | Compile the gate, replay CPU sections 4-6 (cancelled-normal plane, NaN and `-inf` volume, zero-gradient corner) and assert no origin vertex and zero non-finite components appear |
| `meshing:B1` | CUDA + HIP | `@M.cu:387-393`, `:432-438` / `hip:379-389`, `:428-434` | deferred, not compiled, not runtime-tested. Verified: both backends kill the WHOLE cube when ANY one of its 8 corners has `v.weight <= 0.001f` - `classifyVoxelKernel` sets `cube_idx = 0`, `generateMeshKernel` `return`s - so a corner that takes part in no crossing edge still deletes an otherwise fully measured cube and opens a hole wherever observation stops. **CPU is FIXED (Todo 16)**: a cube is evaluated from its crossing edges and only the edges need two supported endpoints. Measured on the CPU frontier fixture: 3228 of 3788 oracle triangles with 1152 boundary edges under the whole-cube rule, 3788 with 0 boundary edges after | Mesh from crossing-edge support per corner, matching the CPU canonical rule | Cracks and holes at every partial-coverage frontier; the emitted mesh is not closed | Compile the gate and replay CPU sections 1-3 on the backend: triangle count equals the independent oracle and boundary edges are zero |
| `meshing:B2` | CUDA + HIP | `@M.cu:357-358` with the outer-shell early returns `@M.cu:377`, `:413` / `hip:353-354` with `hip:373`, `:409` | deferred, not compiled, not runtime-tested. Verified: at the volume shell the gradient's missing neighbour is filled with the CONSTANT `1.0f` by the out-of-range guard inside `computeNormalGPU`'s `sample` lambda - there is no one-sided branch anywhere in either file, and no out-of-bounds read either. **CPU is FIXED (Todo 16)**: `voxelNormal` `@M.cpp:100` uses a one-sided difference on the axis that has no two-sided support (`@M.cpp:127`) and keeps the `0.5` factor on the central form, so a border axis is never weighted twice an interior axis of the same slope. Measured: all 76 border-ring vertices had normals tilted off the analytic `(+1,0,0)` before, zero after | Add the one-sided fallback CPU now has | Every vertex on the volume shell carries a normal biased by a fabricated far-outside sample, so shell shading disagrees with the CPU canonical mesh | Compile the gate and replay CPU section 7: border-ring normals match the analytic direction |
| `meshing:B3` | CUDA + HIP | `@M.cu:363-367`, `:465` / `hip:359-362`, `:461` | deferred, not compiled, not runtime-tested. Verified: `computeNormalGPU` is a central difference with NO voxel-size scale and `+ 1e-9f` inside the `sqrtf`, so a zero gradient returns `(0,0,0)` instead of failing; the same `+1e-9f` pattern re-normalises the blended edge normal, and nothing at emission tests normal length. Corrected against the earlier note: there is NO fabricated `(0,0,1)` in either backend - that CPU-only fabrication is gone from CPU too. So both backends write a zero-length normal into the mesh as a legitimate vertex normal. **CPU is FIXED (Todo 16)**: a collapsed or non-finite blend refuses the edge and every row touching it | Refuse the vertex instead of emitting a zero-length normal | Zero-length normals reach the exported mesh, shade undefined, and are indistinguishable from measured data | Compile the gate and replay CPU sections 4 and 6: zero non-unit normals and no vertex on a cancelled crossing plane |
| `meshing:B4` | CUDA + HIP | `@M.cu:443-459` / `hip:439-455`; the only per-cube cache is `corner_vals[8]` at `@M.cu:435` / `hip:431` | deferred, not compiled, not runtime-tested. Verified: corner normals are recomputed inside the per-edge loop, twice per crossing edge, with no corner-normal cache - up to 24 `computeNormalGPU` evaluations for the same 8 corners, each corner recomputed up to 3 times, and every evaluation costs 7 voxel reads. **CPU is FIXED (Todo 16)**: at most 8 evaluations per cube, cached per corner | Cache a `float3 corner_n[8]` per cube the way CPU now does | Roughly 3x redundant gradient work in the hottest meshing kernel; the backend stays slower than CPU for no behavioural gain | Compile the gate with a call counter and show at most 8 `computeNormalGPU` calls per meshed cube with byte-identical mesh output |
| `meshing:C3` | CUDA + HIP | `@M.cu:454-456`, `:486` / `hip:450-452`, `:484` | deferred, not compiled, not runtime-tested. Verified by exhaustive grep of both files: `isnan` appears exactly once, `isfinite` and `isinf` appear ZERO times. The single guard tests only the vertex POSITION and only for NaN, so a `+/-inf` coordinate passes through untouched, and it never inspects the normal or the color. **CPU is FIXED (Todo 16)**: one finite test covers position and both endpoint normals and the blend, and the poisoned-volume fixture proves a NaN and `-inf` volume now reaches `MeshData` with zero non-finite components | Use `isfinite` over all three outputs on both backends, as CPU does | Infinite coordinates reach the mesh unconverted while NaN is merely relocated - two different failures, neither of them rejected | Compile the gate and replay CPU section 5: zero non-finite components in positions and normals |
| `meshing:C7` | CUDA + HIP | `@M.cu:356-361` against `:391`, `:435` / `hip:352-357` against `:387`, `:431` | deferred, not compiled, not runtime-tested. Verified: the gradient path maps an unobserved voxel to the canonical `+1.0f` sentinel, but the corner-sign and corner-value reads use the raw stored `v.tsdf` with no sentinel and no weight test. That is safe TODAY only because of the whole-cube gate in `meshing:B1`; the moment B1 is relaxed to the CPU crossing-edge rule, an unobserved corner's stale negative literal is read as inside material and mints geometry. CPU fixed both in one change (Todo 16): a single `sampleCorner` feeds the cube sign, the interpolation and the gradient, which is why B1 and C7 must be ported together and not separately | Use one sampler for every consumer | Latent stale-negative geometry: porting B1 without C7 converts frontier holes into invented surface | Compile the gate and replay CPU section 1: a stale-negative unobserved corner emits no vertex |
| `meshing:D8` | CUDA + HIP | position `t` inside `interpolateEdgeGPU` `@M.cu:346` / `hip:342`, independent second `t` inline at `@M.cu:461-462` / `hip:458`; CPU counterparts for comparison: `@M.cpp:96` inside `interpolateEdge` (`:86`) and a second `t` at `@M.cpp:229-231` in the cube loop | deferred, not compiled, not runtime-tested. Verified: on both backends the position's `t` and the normal/color blend `t` come from two separate evaluations of the same closed form, with different guard shapes - the `fabsf(diff) < 1e-6f` early-return chain versus the ternary - and `interpolateEdgeGPU` validates only its scalar inputs, never the `float3` endpoints. The two values agree today only because both clamp the same formula. **CPU IS FIXED HERE (big-fix Todo 17)**: `MarchingCubes::crossingFromLower` now computes `t` exactly once per emitted edge and returns it beside the position, and the cube loop threads that single value into the blended normal and RGB color; the old `interpolateEdge` and the second inline `t` are gone, and `tests/mesh_color_invariant_contract.cpp` locks that a color blended at a parameter other than its position's fails. The backends remain the unfixed side for this row | Compute `t` once per emitted edge and reuse that one value for position, normal and color on both backends | Position and its own normal/color can be blended at different parameters whenever either guard branch diverges - a silent shading-versus-geometry skew no current test catches; CPU removed its duplication in Todo 17, so this is now a backend-only divergence | Compile the gate, show one `t` per emitted edge on each backend, and show the backend mesh byte-identical to the CPU canonical fixture |
| `meshing:C10` | CUDA + HIP | `@M.h:39-41` (CUDA)/`:52-54` (HIP); `initGPU` early return `cuda:502`/`hip:500`, allocs `cuda:509-511`/`hip:507-509`; `freeGPU` `cuda:525-531`/`hip:523-529` only from `@M.cpp:27-33`; per-extraction memset `cuda:542`/`hip:540`; `@PC.cpp:338`, `:310`, `:281`, `:293` | deferred, not compiled, not runtime-tested. Only `d_voxel_tri_counts_` is cleared per extraction; the three mesh output buffers and the offsets are never memset and are freed only by the destructor. No reset path notifies `MarchingCubes` and `TSDFVolume::reset()` never calls `syncToGPU()`, so a live `d_voxels_` keeps pre-reset data that `extractGPU` reads at `cuda:544`/`hip:542` - masked today only because `reset()` starts with `stop()`. Since the CPU empty-sentinel todo, `setParams()` clears the host mirror on ANY parameter difference rather than only a resolution change, and it still never notifies the device, so the host/device divergence window widened to every parameter change | Clear or resize the mesh outputs per extraction and make a volume reset invalidate the meshing device state | VRAM retains the previous, possibly larger, mesh after a reset; a reset path that skips `stop()` publishes a stale mesh | Compile the gate, reset then re-extract, compare against the CPU-canonical empty-then-refill sequence |
| `meshing:SYM` | CUDA | `@M.cu:517-520` `cudaMemcpyToSymbol` x4, `:552-556` two 4-byte D2H reads, `:572-574`, `:611`; HIP wraps all four at `hip:515-518` | deferred, not compiled, not runtime-tested. the four table uploads are entirely unchecked in CUDA while HIP wraps all four - a genuine asymmetry. A failed upload leaves the `__constant__` arrays zeroed and meshing silently emits nothing. At `:552-556` `CUDA_CHECK` only prints, so on failure `total_tris`/`last_count` are uninitialized stack that immediately sizes host vectors and bounds the weld loop; the byte counts are the literal `4` | Wrap the four uploads like HIP does, and check (or derive on device) the two reads before use | Silent empty mesh, or host vectors sized from garbage | Compile the gate, fault-inject the uploads and reads, assert the CPU-canonical error contract |
| `meshing:HYG` | CUDA + HIP | `@M.cu:4`+`:10` duplicate include, `:513`/`hip:511` stale table comment, `hip:13`, `:21` `"CUDA error"` inside `HIP_CHECK`, weld hashers `cuda:596-601` vs `hip:597-604`, `std::unordered_map` at `cuda:604`/`hip:608` with no `<unordered_map>`, `<cmath>` or `<functional>` include | deferred, not compiled, not runtime-tested. The stale comment points at a file that no longer holds a table (moved to `@MT.h` in `7151f5f`); the HIP macros print `CUDA error at ...` while calling `hipGetErrorString`; the two weld hashers differ between backends; neither TU includes what its host-side weld needs, and no backend compile gate has ever exercised it | Dedupe the include, fix the provenance comment, print `HIP error` in the HIP macros, share one weld hash, add the missing includes | A future run reads HIP code as CUDA, and the first HIP compile fails on headers no gate has exercised | Compile the gate and show each named symbol/header corrected or removed |
| `meshing:STREAM` | CUDA + HIP | `@V.cu:203`, `:382`, `:461`, `:479` / `hip:199`, `:379`, `:458`, `:483`; `@I.cu:312`, `:313`, `:322`, `:375`; stream created `@PC.cpp:114`, `:149`, destroyed `:278`, `:290` | deferred, not compiled, not runtime-tested. every TSDF launch ends in a device-wide synchronize, and the ICP launches ride the legacy default stream, while the pipeline's dedicated `cuda_stream_` never reaches a single launch | Launch on the pipeline stream and replace device-wide syncs with stream-scoped waits | Forced serialization and implicit global sync between producer and consumer work - latency the CPU lane cannot expose | Compile the gate, show every launch carries the stream and no device-wide sync remains on the hot path |
| `tracking:GPU-1` | CUDA + HIP | `@I.cu:81` (guarded dest), `:92-94` (unguarded src) / `hip:91-93`; `n` at `cuda:283` vs launch `cuda:312` / `hip:283`, `:312`; caller `@PC.cpp:608`; write at `cuda:42` | deferred, not compiled, not runtime-tested. the downsample source index is dereferenced with no bound test (latent - transitively in-bounds today). The live exposure is different: buffers are sized from `sensor::FRAME_W/FRAME_H` while `width`/`height` come from the frame and are never checked, so any frame at or above the allocation writes out of bounds on both backends | Validate the incoming frame size against the allocation and bound-check the downsample source index | Out-of-bounds device writes on any non-640x480 input; the CPU path builds its own sized `FrameData` and cannot show this | Compile the gate, feed an oversized frame, assert rejection instead of an OOB write |
| `tracking:GPU-2` | CUDA + HIP | `@PC.cpp:606`, `:613`, `:640`, `:647`, `:656`, `:664`; `@S.h:86`; lazy alloc `@S.cu:422-429`; early return `@S.omp:264`; consumer `@I.cu:41`; contract note `include/sensor/Preprocessor.h:33` | deferred, not compiled, not runtime-tested. `getGPUDepthMeters()` can be null (lazily allocated, and `process()` returns early on a size mismatch without allocating), the launch gate is `use_gpu_` rather than the active backend, and `trackGPU` has no null check anywhere - so a null `d_depth` reaches `computeVerticesKernel` | Null-check every device pointer at the `trackGPU` entry and fall back to CPU instead of launching | A size mismatch or a skipped preprocess produces a launch that reads address zero where CPU returns an empty frame | Compile the gate, skip preprocessing, assert the backend degrades instead of launching |
| `tracking:GPU-4` | CUDA + HIP | `@I.cu:312`, `:313`, `:322`, `:324`, `:362`, `:375`, `:393`, `:396`; `_hip.hip` mirrors with `(void)` casts; producer stream `@PC.cpp:114`; retry callers `:600-667` | deferred, not compiled, not runtime-tested. one memset + one device synchronize + one D2H per iteration, x19 iterations (`max_iterations {10,5,4}`) x up to 3 `trackGPU` calls per frame during relocalization; the `:324` sync is redundant (same stream as consumers) yet still stalls the host; the host read at `:395-396` is correctly preceded by the sync at `:393`, so the defect is frequency plus unchecked returns | Launch on the pipeline stream, drop the redundant sync, hoist the per-iteration memset/sync/copy out of the loop | Forced serialization on the tracking hot path, and launch failures surfacing later as unrelated errors | Compile the gate, profile one frame, show one synchronization per level rather than per iteration |
| `tracking:GPU-5` | CUDA + HIP | `@I.cu:297-298` (body `:305-338`), `computeHessianKernel` `:121-274` / `hip:298`; `@I.h:80`, `:90`, `:45`, `:50`, `:53`, `:63`; caller `@PC.cpp:641`, allocs `:121`, `:156` | deferred, not compiled, not runtime-tested. `d_rgb` is never referenced in the function body on either backend and model colors are never read by the Hessian kernel, so the 1.2 MB per-frame device allocation is dead while the caller still pays to fetch the pointer. Colour-based ICP does not exist on either GPU backend | Either consume `d_rgb` in the correspondence stage or delete the parameter and the model color buffers | A parameter and a per-frame allocation advertise a capability that does not exist | Compile the gate, show the parameter consumed or deleted |
| `tracking:GPU-7` | CUDA + HIP | `@I.cu:364`, `:368`, `:395-396`, `:424-426`, `:434-435`, `:451`, `:467-469` / `_hip.hip` same lines | deferred, not compiled, not runtime-tested. the whole Hessian solve is host-side in the GPU path - two 4x4 inversions, a 6x6 eigendecomposition, an `ldlt` solve and a Jacobi SVD, per iteration, up to 19 times per call. The GPU contributes only the assembly | Move the small solve, or at least the re-orthonormalization, on-device, or stop advertising GPU-resident ICP | Per-iteration host round trips dominate tracking, and the README GPU-resident claim is untrue of the solve | Compile the gate, compare pose traces with the CPU canonical fixtures and profile the host/device split |
| `tracking:GPU-8` | CUDA + HIP | `@I.cu:467-469` / `hip:467-469` vs `@IS.h` + `@I.cpp` shared projection | deferred, not compiled, not runtime-tested. CLAIM UPDATED after big-fix Todo 13: CPU now consumes the shared determinant-corrected `projectToSO3` helper in `@IS.h` and `@I.cpp`, so the CPU half of the old shared defect is closed; CUDA/HIP still do bare `svd.matrixU() * svd.matrixV().transpose()` at the cited lines and can return `det = -1`, a reflection. The required backend form is `U*diag(1,1,det)*Vt` | Consume the shared `projectToSO3` helper on CUDA/HIP and remove the bare `U*Vt` reconstruction | A reflected 'rotation' is not in SO(3) and corrupts every later projection on the unverified backends; CPU is fixed, so the remaining risk is backend-only | Compile the gate, assert `det(R) > 0` and `\|det(R)-1\| < eps` on all three paths |
| `tracking:ALL-2` | CUDA + HIP | `@I.cu:419-435`, `:424-427`, `:438`, `:443`, `:450`, `:473`, `:417`, `:336`, `:359`, `:261`, `:372`/`.hip:257-260`; CPU `@I.cpp:64`, `:67`, `:76`, `:100`, `:62`, `:37`, `:125`; `@I.h:19-23` | deferred, not compiled, not runtime-tested. Equal after big-fix Todo 13: Huber `k = 0.02`, valid-depth `0.001`, inlier abort `< 10`, tracking-ok `> 100`, translation cap `0.2`, convergence `5e-5`, shared header defaults, and the CPU adaptive damping policy (`0.01f` level-0, `0.1f` coarse, `1.0f` escalation under `cond > 1e7f \|\| min_eig < 1e-4f`) now lives in `@IS.h`. Backend gap: the GPU code still carries the same damping formula locally, is not compiled/runtime-tested, and has not consumed the shared helper. Remaining CPU/backend differences: Rodrigues `1e-6f` vs `1e-4f`; GPU rotation cap remains unverified while CPU now enforces `0.5 rad`; pi as `3.14159f` vs `M_PI`; CUDA shared memory `s_reduce[8][34]` with exactly 8 warps for the `dim3(16,16)` launch and floor-divided `num_warps` (a non-multiple-of-32 block silently loses a warp) vs the portable `warpSize`-based HIP twin | Consume the CPU canonical `@IS.h` adaptive damping, condition test and step caps on CUDA/HIP behind one constant set; `ICPParams` exposes no damping knob, so it is unreachable from `FusionHyperparams` | The CPU fixed point is locked by `tests/icp_numeric_policy_contract.cpp`; the GPU fixed point is unverified because the backend solve has never been compiled or runtime-tested | Compile the gate, compare backend ICP pose/energy traces against the CPU canonical fixtures |
| `tracking:HDR-1` | CUDA + HIP | claimed HDR path in the tracking stage | deferred, not compiled, not runtime-tested. CLAIM WITHDRAWN AS FALSE: grep for `HDR`/`hdr` across `src/` and `include/` returns zero hits - no HDR code exists | Nothing; close the ID in the audit draft so a future run does not hunt for it | A phantom feature in the audit memory invites fixing code that does not exist | Re-run the grep and confirm the ID stays closed |
| `tracking:A10`, `tracking:A11`, `tracking:A17` | CUDA + HIP | rotation cap `@I.cu:442-445`/`.hip:442-445` vs CPU `@I.cpp` Todo 13 guard and the post-hoc `@PC.cpp:692`; model gate `cuda:181-182` vs `@IS.h` shared squared-norm predicate (wrong `hip:179` comment); live normal gate `@IS.h` + `@I.cpp`, no GPU counterpart; counters `cuda:411-415` vs `cpp:276-280`, `cuda:157` vs `cpp:186`, `:193`, `cuda:162`+`:168` vs `cpp:200-201`; gate spaces `cuda:193-198`, `:205-206` vs `cpp:221-222`, `:232-233` | deferred, not compiled, not runtime-tested. The GPU code still branches on a rotation step above 0.5 rad but is unverified; CPU now rejects any non-finite, `norm(t) > 0.2`, or `norm(omega) > 0.5` update without mutating the pose. The GPU model gate demands `\|\|n\|\| > 0.9487` while CPU now gates model vertices with finite and `squaredNorm(v) > 1e-12` and model/live normals with finite and `squaredNorm(n) > 0.9` through `@IS.h`; the old CPU-only `live_n.z() == 0.0f` test is gone. Counters still diverge for the 1-pixel border, `valid_live` placement, and backend gate spaces - GPU in reference-camera space, CPU in world space - against the same `0.1 m`/`30 deg` constants | Port the CPU rotation cap and shared validity predicates to CUDA/HIP, and evaluate the gates in one named space on all three paths | `metrics_.icp_overlap_pct` and the `--verbose` advice thresholds are not comparable across backends, so a parity run chases a phantom difference | Compile the gate, compare counter by counter against the CPU canonical ICP fixtures |
| `tracking:A13` | CUDA + HIP | `@I.cu:222-238` (guard `:221`, weight `:223-225`, curvature `:228-230`, gradient `:232`, objective `:234`); `@I.hip` mirrors; CPU `@I.cpp` Todo 12 block (`:271-293` + `LocalAcc::add` `:179-192`) and `include/tracking/ICPShared.h` | CPU CANONICAL ESTABLISHED by big-fix Todo 12: with `w = min(1, kHuberK/abs_err)` and `kHuberK = 0.02f` shared in `@IS.h`, the IRLS/MM triple is curvature `A += w*J*Jt`, gradient `b -= J*(w*e)`, reported objective the true Huber loss `psi` (`huberLossFromAbs`), locked by `tests/icp_weighting_contract.cpp` (t_z = -0.12/11.25 under canonical Todo 13 damping, energy 0.0038/11). CLAIM CORRECTED TWICE: the original 'CPU unweighted vs GPU weighted' divergence framing is FALSE (both accumulated `(err*w)^2`), AND this row's earlier correction - 'Huber-IRLS requires `w^2*JtJ`' - is ALSO FALSE and withdrawn: `w^2*JtJ` pairs with a `w^2*e*J` gradient, not the `w*e` gradient all three files compute, and `(w*e)^2` is bounded by `4*k^2` so it cannot majorize the unbounded Huber loss it reports; the canonical curvature weight is `w`. The stale `w^2` claims in `.omo/evidence/big-fix/backend-verification-icp-sensor.txt` are historical and left unrewritten; this row supersedes them. Backends untouched (deferred, not compiled, not runtime-tested): unweighted curvature, `J*(err*w)` gradient, `(err*w)^2` objective, inline `huber_k = 0.02f` | Port the CPU canonical triple in one formulation shared by both backends: weight the curvature by `w` (NOT `w^2`, NOT 1), keep `b -= J*(w*e)`, report `psi` instead of `(err*w)^2`, and consume the `kHuberK`/`huberLossFromAbs` single source of truth instead of inline literals | Curvature, gradient and reported energy disagree with each other and with the CPU estimator, so backends take over-confident steps on outlier-heavy frames and converge to a different fixed point than the CPU canonical tracker; fixing `w^2` per the withdrawn claim would have manufactured a THIRD wrong fixed point | Compile the gate, then assert backend Hessian, gradient and reported objective match the CPU canonical fixture of `tests/icp_weighting_contract.cpp` (t_z = -0.12/11.25 under canonical Todo 13 damping and psi sum 0.0038/11) within the documented backend tolerance |
| `tracking:ALL-3` | CUDA + HIP | `@I.cu:221` / `@I.hip:220` Jacobian guard; no residual test anywhere in either kernel; CPU `@I.cpp:259-286` (`isfinite(err)` before the weight, all six `J` components guarded) | CPU CANONICAL ESTABLISHED by big-fix Todo 12: a correspondence accumulates only when the point-to-plane residual is finite AND all six Jacobian components are finite; a rejected correspondence leaves no trace in the Hessian, gradient, objective or inlier count. The residual test is mandatory and ordered before the weight: a NaN model depth yields a NaN residual while all six `J` entries are finite, so a Jacobian-only guard cannot catch it. Locked by `tests/icp_weighting_contract.cpp` (an `(0,0,+Inf)` model normal and a NaN model depth each leave the 11 valid correspondences' pose and objective bit-identical to the clean run). Both backends still guard only `!isnan(J[0]) && !isinf(J[0]) && !isnan(J[3]) && !isinf(J[3])` and never test the residual (deferred, not compiled, not runtime-tested) | Add the residual-finite test before the Huber weight and widen the Jacobian guard to all six components in both backends, keeping the same reject-without-trace postcondition; in the CUDA kernel the guard must stay in the non-`continue` form (see the KIN-FORK comment at `@I.cu:219-220`) | One corrupted model vertex or normal poisons the device Hessian/gradient through paths the CPU now closes; NaN reaches the pose solve and the backend pose stops reproducing the clean run | Compile the gate, run the CPU canonical corrupted-correspondence fixtures from `tests/icp_weighting_contract.cpp`, and assert each backend result equals the same fixture with the corrupted correspondence removed |
| `tracking:A30` | CUDA + HIP | the audit draft's blanket 'constants verified equal' claim; `@I.h:22-23`; `@FP.h:26-27`; dead helpers `@I.cpp:15-18` vs use at `:204-205` | deferred, not compiled, not runtime-tested. CLAIM WITHDRAWN as a blanket statement (see `tracking:ALL-2` for the real equal/different split). Big-fix Todo 13 moved the canonical CPU damping, cap, validity, projection, and angle-sanitization constants into `@IS.h`, but `ICPParams::min_depth`/`max_depth` are still assigned from `FusionHyperparams` and read by none of `@I.cpp`, `_cuda.cu` or `_hip.hip`; `getFx/getFy/getCx/getCy` still have zero call sites while `buildLinearSystem` hard-codes the full-resolution intrinsics | Replace the blanket claim with the per-constant table, and either honor or delete the depth fields and the dead per-level intrinsic helpers | A dead per-level intrinsic helper invites a future 'fix' that breaks the currently-correct full-resolution model projection, and two GUI-fed depth fields do nothing | Compile the gate, then re-derive the constant table from source rather than from this dossier |
| `tracking:A31` | CUDA + HIP | `@I.cu:169-170`, `:331`, `:353-357` vs `@I.cpp:207-208` | deferred, not compiled, not runtime-tested. `__float2int_rd` floors and `static_cast<int>` truncates, so they disagree for negative operands (`-5.2` -> `-6` vs `-5`); both are then rejected by `mx >= 0`, so the impact is a slightly different `projected_points` count, but the paths are not bit-compatible. Separately the GPU projects with full-resolution intrinsics at every level while scaling live vertices by `width >> level`, so coarse levels carry an effective centering error - and CPU does the same thing through the dead helpers | Adopt one rounding primitive, and either scale intrinsics per level or document that full-resolution centering is intended | Sub-voxel correspondence drift that grows with pyramid level - invisible in pose, visible in counters | Compile the gate, compare backend voxel-index traces against the CPU canonical conversion table |
| `tracking:A32` | CUDA + HIP | `@I.cu:272` / `hip:272`, block reduce `:257-271`; CPU `@I.cpp:264-281` | deferred, not compiled, not runtime-tested. the GPU Hessian is assembled by `atomicAdd` of 34 floats per block over ~300 blocks, so the fp32 accumulation order varies frame to frame and the pose is not bit-reproducible; CPU sums thread-local accumulators in a fixed order and is deterministic | Use a deterministic reduction if bit-reproducibility is required, or declare the backend non-deterministic and set an explicit tolerance | The CPU lane claims byte-identical repeats and the backend cannot honor it; an unannounced tolerance hides real regressions | Compile the gate, run one input N times and publish the observed pose spread as the backend tolerance |
| `tracking:A33` | CUDA + HIP | `@I.cu:12-18` macro, uses at `:362`, `:393`, `:396`; `h_hessian[34]` at `:395` consumed `:398-408`; eigensolver `:424`; `hip:11-25`, `:362`, `:393`, `:396` | deferred, not compiled, not runtime-tested. `CUDA_CHECK`/`HIP_CHECK` print and continue, and `trackGPU` has no failure return anywhere. If the D2H at `:396` fails, a 34-float uninitialized stack array becomes the Hessian, the inlier counters and the diagnostics, and can surface as NaN or an assert in the eigensolver | Return a status on any failed API call and leave the previous pose untouched | One failed copy becomes a garbage pose update instead of a tracking failure | Compile the gate, fault-inject the D2H, assert the backend reports failure per the CPU canonical contract |
| `tracking:A34` | CUDA + HIP | GPU pyramid `@I.cu:95-99` vs level-0 guard `:58-61`; CPU `@FD.cpp` `downsample()` jump guard (added big-fix Todo 12) vs `@IS.h` `depthJumpThreshold` | CPU CANONICAL ESTABLISHED by big-fix Todo 12: the CPU pyramid `downsample()` now applies the shared `depthJumpThreshold(d_min) = max(0.03, 0.05*d_min)` guard (from `@IS.h`) and, on a discontinuity, keeps the nearest (smallest-depth) valid sample instead of averaging both surfaces into a ghost vertex; smooth blocks still average, sentinel `0` samples are still excluded. GPU pyramid is untouched (deferred, not compiled, not runtime-tested) and STILL has no downsample guard, so the GPU ghost-vertex defect is now a genuine CPU/GPU divergence (it was shared before Todo 12) | Apply the shared depth-jump guard inside the GPU `downsampleKernel` at every level, matching the CPU nearest-sample selection (deterministic min-depth pick), sourced from `@IS.h` | Ghost vertices at silhouette edges propagate through the whole GPU pyramid while CPU is now clean, so coarse-to-fine ICP input diverges CPU-vs-GPU | Compile the gate, compare a discontinuity fixture downsampled at levels 1-2 against the CPU canonical pyramid fixture in `tests/icp_weighting_contract.cpp` |
| `sensor:S-01`, `sensor:S-02` | CUDA + HIP | `@S.cu:403-407`/`.hip:404-408`, call site `cuda:477`, pass-through filters `cuda:219-222`, `:357-360`, sentinel `cuda:69`, clamp `cuda:76`; consumer `@I.cu:41-43`; CPU `@K.h:26-29`, `@FD.cpp:105-109`; pipeline `@PC.cpp:496-497`, `:507`, `:640` | deferred, not compiled, not runtime-tested. `rawToMetersKernel` takes no band parameters though both are in scope at the call site, and the GPU feeds `getGPUDepthMeters()` straight into `trackGPU`, bypassing `buildFrameData` - the only place the `[min,max]` band is enforced. The GPU filters preserve out-of-range depth and `computeVerticesKernel` accepts any `d > 0.0f`, so out-of-band depth becomes geometry. The zero-sentinel chain is sound (`rawDepthToMetersGPU(0) == rawDepthToMetersGPU(2047) == 0.0f`, `metersToRawDepthGPU` clamps to `[1,2046]` so hole fill cannot invent a 2047); predicate is `>= 2047` on CPU vs `== 2047` on GPU, equivalent only because the sensor is 11-bit | Give `rawToMetersKernel` the configured band and zero out-of-band output so the GPU feeds the CPU canonical validity rule | The GPU manufactures geometry from depth the CPU rejects, through a path the CPU cannot observe | Compile the gate, compare GPU depth-to-meters against the CPU canonical validity table over 0..2047 raw |
| `sensor:S-04` | CUDA + HIP | `@S.cu:312-315`, `:331`, radius `:27` / `hip:313-316`; CPU `@S.omp:481-483`, `:502`, radius `:26` | deferred, not compiled, not runtime-tested. the GPU treats raw `{0, 2047}` as holes, CPU treats only raw `0`, so the Kinect saturated/reflective sentinel is in-filled on GPU and left alone on CPU. Both map 2047 to 0.0 m, so the pixel is invalid either way - but the GPU has overwritten a measured sample with an interpolated neighbour, which then propagates through the spatial-denoise and guided-filter windows and changes neighbouring outputs. Radius and weighting are equal | Make the GPU hole predicate raw `0` only, matching the CPU canonical invalid-depth rule | Radiometrically different point clouds per backend from the same frames, upstream of every consumer | Compile the gate, feed a raw-2047 fixture, assert the GPU leaves it untouched |
| `sensor:S-11`, `cross-backend:B4` (verified detail) | CUDA + HIP | `@R.cu:67-96` `static CudaBufferRAII g_rcas_buffers`; `@R.hip:16-43` with statics at `:45` and `:233`, design comment `:13-14`, mutex locks `:141`, `:242` | deferred, not compiled, not runtime-tested. three namespace-scope statics with non-trivial destructors run `cudaFree`/`hipFree` at static-destruction time during `exit()`, after Qt teardown and possibly after the runtime has dropped its primary context (`cudaErrorCudartUnloading` on CUDA, a hang/crash class on ROCm). There is no `atexit` ordering control, no explicit shutdown, no sync before free, cross-TU order is unspecified, and `HipBufferRAII` holds a mutex that can be destroyed while held | Replace the process-lifetime singletons with pipeline-owned, teardown-ordered buffers | Teardown hang or crash at exit, and unbounded buffer lifetime across re-init | Compile the gate, run repeated init/shutdown cycles, assert bounded lifetime and deterministic teardown |
| `sensor:S-17` | HIP | `@S.hip:410-414` (correct `HIP error` macro at `:16`); `@S.h:19-25`, `:33`, `:57`, `:73`; `@S.omp:262`; `@PC.cpp:363` and the `cuda_stream_` member; `@P.cpp:96-97` | deferred, not compiled, not runtime-tested. The exported HIP-path symbol is `processCuda`, the HIP stream alias is `cudaStream_t`, the parameter is `cuda_stream`, the pipeline member is `cuda_stream_`, and a HIP build constructs a class named `CUDAPreprocessor` for `PreprocessBackend::HIP`. `@S.hip:16` and `@I.hip:23` print `HIP error` correctly, so the mislabel is naming-only plus `@M.hip` (`meshing:HYG`) | Rename the backend-neutral entry points and stream alias, or document the aliasing once, and name the preprocessor class for what it serves | Developers debugging ROCm read CUDA names in stack traces and disassembly, and grep-based audits miss HIP code | Compile the gate and show the HIP path's public names no longer claim to be CUDA |
| `sensor:S-18` | CUDA + HIP | `@R.hip:70-79` `cubic()`, `:81` `easuKernel`, `:91-92`, `:101-116`, `:127` `applyEASU_GPU`; CPU `@R.cpp:18-19`, `:21`, `:42-50`, `:77-93` | deferred, not compiled, not runtime-tested. The pass labelled EASU is a fixed isotropic separable 4x4 Catmull-Rom bicubic resample: versus real FSR 1.0 EASU it has no local min/max edge-direction estimate, no anisotropic ellipse kernel, no `tanDistSq`, no gradient vectors, no edge-clamped footprint and no 12-tap pattern. The CPU twin claims 'edge-adaptive sampling' while implementing none, and cites a paper algorithm the code does not contain | Implement the missing edge-adaptivity, or rename the pass and delete the citation (the CPU SR-contract todo owns the naming) | Ringing and halos at 2x-4x, plausibly the 'black textures' complaint behind the disabled upscaled path, under a name advertising a capability the code lacks | Compile the gate, compare a fixed edge fixture against the CPU canonical super-resolution reference |
| `sensor:S-19` | CUDA + HIP | `@S.cu:253-304` (write `:303`, neighbour read `:282`); `hip:254-305`; CPU `@S.omp:450` vs `:429` | deferred, not compiled, not runtime-tested. the temporal EMA writes `depth[idx]` in place while other threads read neighbours' current-frame values from the same array, so which neighbours a thread sees depends on scheduling and the reset decision uses partially updated data - output differs every frame. Every other depth pass ping-pongs `d_depth_in_`/`d_depth_out_`; only the EMA is in-place, and the CPU OpenMP version has the identical race | Write the EMA to the alternate buffer and swap, exactly as the other passes already do | Non-reproducible depth from identical input on all three paths; it breaks the CPU lane's own determinism claim until the CPU todo fixes it | Compile the gate, assert N repeats of one frame are bit-identical per the CPU canonical determinism rule |
| `sensor:S-20` | CUDA + HIP | `@S.cu:419`, `:470`, `:472`, kernel param `:403`, call `:477`; `hip:420`, `:474`, `:481` | deferred, not compiled, not runtime-tested. a `size_t` element count is narrowed to `int` in the launch arithmetic and again in the kernel parameter - a frame-size assumption baked into the launch math that silently truncates above 2^31 | Keep the count widened through the launch config and the kernel signature | Latent truncation today (fixed 640x480 sensor) that becomes live for any higher-resolution input | Compile the gate and show no narrowing between the host count and the kernel index |
| `sensor:S-21` | CUDA + HIP | `@S.cu:27` (`int kGuidedRadius`), `:383`, sigmas `:384-385`; CPU `@S.omp:29` and the stale comment at `:523-526` | deferred, not compiled, not runtime-tested. the guided filter uses the integer window radius as a Gaussian sigma, coupling spatial falloff to window size, so the recorded reduction of `kGuidedRadius` from 9 to 4 changed the filter's shape, not just its cost, while the luma and depth terms correctly use named sigmas. Constants are equal across backends, but the CPU comment still describes a `radius=9` / 19x19 kernel that exists in neither file (both use 4) | Introduce a named spatial sigma and correct the stale CPU comment | A future window-size tune silently retunes the filter shape, and the comment documents a kernel that is not in the file | Compile the gate, compare a guided-filter fixture against the CPU canonical reference |
| `sensor:S-22` | CUDA + HIP | `@S.cu:190-198` (RGB median, bound hard-wired `i < 5`) vs `:238-246` (depth median, uses `count`); CPU `@S.omp:155` `std::nth_element` | deferred, not compiled, not runtime-tested. three formulations of one median. All agree only because the window is always full; the GPU RGB bound assumes `count == 9` with no assertion, and the two GPU medians are written differently for the same task | Use one count-correct median formulation on all three paths | Only accidentally equivalent - any border or incomplete-window case diverges | Compile the gate, compare median output on an incomplete-window fixture against the CPU canonical reference |
| `sensor:S-23` | CUDA + HIP | `@S.cu:138-140` raw-byte reads against `kRgbSigmaRange = 28.0f` (`:35`) in `color_dist_sq` (`:155-157`), versus `[0,1]` in `rcasGetPixelCUDA` (`:94-96`) and `/255` in `computeLumaKernel` (`:209`), and the `1.0f - mx` headroom at `:118` | deferred, not compiled, not runtime-tested. three numeric ranges for the same RGB buffer coexist in one file. Each kernel is internally consistent, but `kRgbSigmaRange = 28.0` is only meaningful in 0-255 while the FSR epsilon and the `1.0f - mx` headroom are only meaningful in `[0,1]`, so the constants are incommensurable | Declare one color range per buffer in the type or a named constant, and make each kernel's expectation explicit | A refactor that 'unifies' the ranges silently rescales a sigma by 255x and changes denoise strength | Compile the gate, compare bilateral and RCAS output against the CPU canonical color-range reference |
| `cross-backend:A21`, `A22`, `A23`, `A24`, `B5` (verified detail) | CUDA + HIP | bilateral `@S.cu:169-171`/`.hip:170-172` vs CPU `@S.omp:207-209` + `clampToByte` `:96-98`; `return true` `cuda:485`/`hip:489`, allocs `cuda:423-429`, throw `@CUP.h:38-41`, unreachable fallback `@S.omp:269-278`, chain `@P.cpp:70` -> `@PC.cpp:501`; RCAS `@R.cu:109`, `:115-118` (zero `CHECK` hits) vs HIP checks `@R.hip:147-150`, `:161-169`, `:248-270`; EASU `@R.hip:131-143` (`dst.resize` `:133`, alloc `:142`, void return), caller `@S.omp:341-349`; sharpness `@R.cu:102-103`/`.hip:239-240` (`std::max`/`std::min`) vs `@R.cpp:128-129` (`std::clamp`), `:158` | deferred, not compiled, not runtime-tested. Detail behind those rows: the GPU bilateral lacks the divide guard, the saturating cast and the source fallback (centre tap keeps `weight_sum >= 1.0`, but a NaN/Inf float-to-`uint8_t` cast is UB where CPU degrades). `processCuda` has one return - alloc failures throw through `@CUP.h`, so the deliberate CPU fallback can never fire and the exception escapes `trackingLoop`, while `initGPU()` is try/catch-guarded: two failure models. CUDA RCAS is less checked than HIP - a failed upload/launch leaves `d_out` holding previous-frame data, which the download writes back over the caller's `rgb`. EASU `dst.resize()` zero-fills 3.69 MB before the allocation, is `void` (no error channel), and sizes to `dst_size` when only `src_size` is needed. Sharpness: `std::min(NaN,1)` then `std::max(0,NaN)` = `0.0f` on GPU vs `std::clamp(NaN,0,1)` = NaN on CPU -> `static_cast<uint8_t>(NaN*255)` UB; values equal today (0.5f) | Add the divide/saturate guards, report failure instead of throwing, raise CUDA RCAS checks to HIP's level, give `applyEASU_GPU` an error channel, make the sharpness clamp NaN-safe on all three paths | Silent stale-frame and black-frame corruption, an unreachable CPU fallback, UB cast on CPU | Compile the gate, fault-inject each named call, compare with the CPU-canonical conditioner and super-resolution contracts |
| `cross-backend:A14` | CPU + CUDA + HIP | CPU `@M.cpp:100`, `:127`, `:222`, `:226`, `:237`, `:254`; GPU `@M.cu:366-367`, `:465`/`hip:362-363`, `:461` | deferred, not compiled, not runtime-tested. **CPU is FIXED (big-fix Todo 16)** and the two remaining paths are now the only carriers. CPU: `voxelNormal` `@M.cpp:100` returns a NaN sentinel for an unusable corner instead of a plausible normal, `:127` is the one-sided border fallback, `:226` rejects a non-finite endpoint normal, `:237` rejects a blend that is non-finite or collapses under `kGradientEpsilon`, and `:254` drops the triangle rows touching a refused edge - so neither a fabricated normal nor a NaN one can reach `MeshData`. CUDA and HIP still have no normal-validity gate at all: their epsilon sits inside the `sqrtf` (`+ 1e-9f`), so the backend never returns zero and never rejects, and a zero gradient silently yields a `(0,0,0)` normal that is emitted as valid. The backends' only cube-level gate is still `vox.weight <= 0.001f` (`meshing:B1`) | Reject a vertex whose interpolated normal is not finite and non-zero on both backends, the way CPU now does, instead of letting `+ 1e-9f` stand in for a decision | A zero-length or undefined normal is indistinguishable from a measured one in the exported mesh, so the defect survives inspection and only shows in shading - on CPU this is now locked by `tests/marching_cubes_frontier_contract.cpp`, on the backends it is still live | Compile the gate, compare per-face normal validity against the CPU canonical outward-normal fixture and CPU sections 4-6 |
| `tsdf:T3` | CUDA + HIP | `@V.cu` / `@V.hip` integration kernel (`integrationKernel_VoxelParallel` per-voxel fusion; the CPU mirror is `@V.cpp` `integrateCPU`) | CPU CANONICAL DETERMINISM ESTABLISHED by big-fix Todo 14: `@V.cpp` `integrateCPU` no longer performs a concurrent read-modify-write of `voxels_[idx]` (which lost updates where ray marches overlap and changed the fused voxel run to run); it is now a two-phase merge - Phase 1 derives each (pixel, step) candidate with no shared write, Phase 2 applies candidates serially in canonical order (increasing y, then increasing x, then increasing march step) through the unchanged weight, TSDF and color formulas, ordered by a lossless raster-then-march sequence key, so the volume is byte-identical across repeated runs and OpenMP thread counts 1 / 2 / 4 and equals the single-thread fold. Locked by `tests/tsdf_integration_race_contract.cpp`. Backends untouched (deferred, not compiled, not runtime-tested): each backend integration kernel still fuses into shared device voxels with no deterministic ordering and no CPU-canonical merge | Port the CPU canonical deterministic merge to CUDA/HIP (a deterministic two-phase or ordered reduction reproducing the CPU raster/march fold) or pin and document the exact reduction order the backend relies on | The backend volume is not reproducible run to run (lost updates where ray marches overlap) and cannot be matched byte-for-byte against the CPU-canonical volume the CPU tests lock, so backend integration parity is unprovable | Compile the gate, run one input N times on the backend, assert byte-identical volume and mesh across repeats and agreement with the CPU-canonical fold within the documented backend tolerance |
## Deferral policy restated

- `deferred` — the finding is real, in scope for a future backend run, and out
  of scope for this CPU-only plan.
- `not compiled` — no `nvcc` / `hipcc` invocation, no `GPU_BACKEND=CUDA`, no
  `GPU_BACKEND=HIP`, no `build-cuda-big-fix`, no `build-hip-big-fix` occurred
  in this run.
- `not runtime-tested` — no backend test ran; CPU tests are not backend proof.
- Existing `build-cuda/` is protected and was not mutated.
